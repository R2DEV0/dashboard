@model dash.Models.RolesViewModel

@{
    ViewData["Title"] = "Roles";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-6">Manage Roles</h1>
        <div>
            <button class="btn btn-secondary" id="backBtn" onclick="location.href='@Url.Action("Index", "Admin")'">
                Back
            </button>
            <button class="btn btn-primary" id="addRoleBtn">
                <i class="bi bi-plus-circle"></i> Add New Role
            </button>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Name</th>
                        <th scope="col" class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var role in Model.Roles)
                    {
                        <tr>
                            <td>@role.Id</td>
                            <td>@role.Name</td>
                            <td class="text-end">
                                <button class="btn btn-sm btn-warning editRoleBtn" data-id="@role.Id">
                                    <i class="bi bi-pencil"></i> Edit
                                </button>
                                <button class="btn btn-sm btn-danger removeRoleBtn" data-id="@role.Id">
                                    <i class="bi bi-trash"></i> Remove
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="addEditRoleModal" tabindex="-1" aria-labelledby="addEditRoleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addEditRoleModalLabel">Add/Edit Role</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body" id="modalContent">
                <!-- The form will be loaded here via AJAX -->
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="/js/roles.js"></script>
}